{
  "name": "blogging-website",
  "version": "1.0.0",
  "scripts": {
    "build": "parcel build src/index.html --dist-dir dist --public-url ./"
  },
  "devDependencies": {
    "parcel": "^2.15.4"
  }
}